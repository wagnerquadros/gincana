If you want to use Firebase Firestore:
1) Go to Firebase Console -> Project Settings -> Service accounts
2) Generate new private key (JSON) and download it.
3) Place the downloaded file as `serviceAccountKey.json` in the project root (next to package.json).
4) Set USE_FIREBASE=true in your .env
